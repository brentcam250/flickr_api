<%= form_with url: root_path, method: :get, local: true do |form| %>
    <%= form.label :flickr_id, "Flickr ID:" %>
    <%= form.text_field :flickr_id %>
    <%= form.submit "Search", :class => 'button'%>

<% end %>